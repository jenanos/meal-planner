# Base URL to reach the backend API (adjust host/port as needed)
NEXT_PUBLIC_API_URL=<your-api-url>
# Sett til "true" for å bruke innebygget mock-modus (ingen backend kreves)
NEXT_PUBLIC_MOCK_MODE=false
