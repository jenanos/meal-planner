-- NOTE:
-- This migration was generated out of order and attempted to alter
-- "ShoppingStore" before that table exists in the migration history.
-- Keep this migration as a no-op so existing history remains valid.
SELECT 1;
